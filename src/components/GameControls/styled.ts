import styled from "styled-components";

export const SCGameControls = styled.div`
  margin: 0 auto;
  background: grey;
  width: 150px;
  > div {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
`;
