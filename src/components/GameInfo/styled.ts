import styled from "styled-components";

export const SCGameInfo = styled.div`
  margin: 0 auto;
  background: gold;
  width: 90%;
  > div {
    display: flex;
    flex-direction: row;
    align-items: space-around;
    justify-content: space-around;
  }
`;
